# Nombre de la aplicación
spring.application.name=proyectoLogisticaCSL

# --- CONEXIÓN BASE DE DATOS (POSTGRESQL) ---
# ¡Asegúrate de que la base de datos 'logistics_db' existe en tu PGAdmin!
spring.datasource.url=jdbc:postgresql://localhost:5432/logistics_db
spring.datasource.username=postgres
# CAMBIA ESTO POR TU CONTRASEÑA REAL DE PGADMIN
spring.datasource.password=tu_contraseña_postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CONFIGURACIÓN GMAIL CSL (CORREGIDA) ---
# CONFIGURACIÓN GMAIL CSL
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# --- CAMBIO 1: TU NUEVO CORREO ---
spring.mail.username=controlsystemlogistic@gmail.com

# --- CAMBIO 2: LA NUEVA CONTRASEÑA DE APLICACIÓN DE 16 LETRAS (Sin espacios) ---
spring.mail.password=okjvnxdmgzoijgrk

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# --- SERVIDOR ---
server.port=8080